# Process Writeup

## Name: Thomas Dono
## Course: APCSA
## Period: 2
## Concept: Boolean Ifs

### Context
I am learning Java in an AP course so that I can get a jump start on what a college workload/class might feel like.

#### What We Learned
We learned how to us If statements with boolean values, how to evaluate boolean circuits, and how to shortcircuit the circuit so we can slove it faster.

There are 3 if statements we learned, `If()`, `If() else`, and `If() else If()`. All the `If()`'s have the condition in the parentheses and they can be set with `x <= 1` x being a variabe, this condition will check if x is less than or equal to 1. `If() else` has it so the if the condition is not met it will exacute some code and `If() else If()` will check a different condition only if the first condition is not met.

To help execute and debug code of the booleans for the if statements we learned how to make a truth table. We used 1 and 0 for true and false repectivly and used bionary to make sure we counted all posible variations to run through the conditions and make sure it runs proporly.

### Challange
The only challenge I faced was making sure I was defining the ranges proporly. For example `If(x <=52 && x>90)` will if x is within the range of 52 inclusive and 90 exclusive. It was hard for me to remember inclusive and exclusive but doing more activities with it helped me remeber and understand it better.

### Takeaways
There are always ways that can help you seeped up how to figure out code and whats wrong with your code. People are always willing to help if you ask and helping others can help you better understand the consepts and help you learn.